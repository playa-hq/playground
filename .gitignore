# --- secrets ---
.env
.env.local
*.local.json
!.*.example

# --- entire (personal overrides) ---
.entire/settings.local.json

# --- agent hooks (per-developer, installed by `entire agent add`) ---
# The repo is agent-agnostic: everyone picks their own. Shared Entire config
# lives in .entire/settings.json, which IS committed.
.claude/
.codex/
.cursor/
.gemini/
.opencode/

# Tool-specific pointers at AGENTS.md. Generated by scripts/setup.sh so the
# repo names no particular agent; AGENTS.md is the file everyone actually reads.
CLAUDE.md
GEMINI.md
.cursorrules

# --- aikido ---
.aikidotmp/

# --- generated media / experiment output ---
out/
outputs/
assets/generated/
*.mp4
*.mov
*.png
*.jpg
*.jpeg
*.webp
*.wav
*.mp3
!docs/**/*.png

# --- deps & build ---
node_modules/
dist/
build/
.next/
__pycache__/
*.pyc
.venv/
venv/
.uv/

# --- editors / os ---
.DS_Store
.idea/
*.swp
