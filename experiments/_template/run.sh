#!/usr/bin/env bash
# Entry point for this experiment. Keep it to one command.
set -euo pipefail
cd "$(dirname "$0")"

[ -f ../../.env ] && set -a && . ../../.env && set +a

echo "TODO: wire this up."
